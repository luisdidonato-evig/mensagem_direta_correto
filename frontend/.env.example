VITE_API_URL=http://localhost:8000/api/v1
# Ative para exibir a tela de acesso e guardar o token somente em sessionStorage.
VITE_AUTH_ENABLED=false
# Conveniência exclusiva para desenvolvimento. Nunca compile um token real no bundle.
VITE_API_TOKEN=
